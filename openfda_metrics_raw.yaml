steps:
  - dataFrameName: fdas
    sql:
      SELECT
        col.*
        FROM (
          SELECT
            explode(results)
          FROM fdas_raw_input)
output:
  - dataFrameName: fdas
    outputType: JSON
    outputOptions:
      saveMode: Overwrite
      path: "fdas_raw_uncompressed.json"